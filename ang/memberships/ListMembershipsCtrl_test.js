'use strict';

describe('memberships module', function() {

  beforeEach(module('memberships'));

  describe('view1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var membershipsListMembershipsCtrl = $controller('MembershipsListMembershipsCtrl');
      expect(membershipsListMembershipsCtrl).toBeDefined();
    }));

  });
});